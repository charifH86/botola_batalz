<div class="login-body">
   <div class="container-login right-panel-active" id="login">
    <div class="form-container sign-up-container ">
      <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
        <h2><strong>SIGN IN</strong></h2>
        <span> Please enter your email and chose a password</span>
        <div class="hidden-label">
          <%= f.input :email,
                  required: true,
                  autofocus: true,
                  input_html: { autocomplete: "email", placeholder: "Email" }%>
          <%= f.input :password,
                  required: true,
                  hint: ("#{@minimum_password_length} minimum lenght" if @minimum_password_length),
                  input_html: { autocomplete: "new-password", placeholder: "password" } %>
        <%= f.input :password_confirmation,
                  required: true,
                  input_html: { autocomplete: "new-password", placeholder: "Confirm your password" } %>
        <%= f.button :submit, "Create my account", class: "loglog" %>
        </div>
      <% end %>

    </div>
    <div class="overlay-container">
      <div class="overlay">
        <div class="overlay-panel overlay-left" id="reglog">
        <br>
          <h1>BWelcome !</h1>
          <p>Sign in and join the competition !</p><br>
          <p>Already a member ?</p><br>
          <%- if controller_name != 'sessions' %>
            <%= link_to "Start", new_session_path(resource_name), class: "ghost" %><br />
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>